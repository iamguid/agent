name: agent_flutter
description: Agent library integrated to flutter environment.
version: 0.1.0
homepage: https://github.com/iamguid/agent

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  agent_core: ^0.1.0
  provider: ^6.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
